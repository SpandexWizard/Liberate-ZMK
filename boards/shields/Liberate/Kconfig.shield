# No whitespace after the comma or in your part name!
config SHIELD_LIBERATE_LEFT
    def_bool $(shields_list_contains,liberate_left)

# No whitespace after the comma or in your part name!
config SHIELD_LIBERATE_RIGHT
    def_bool $(shields_list_contains,liberate_right)
